type User = {
    username: string,
    emails: string[],
    name: string,
    phone: string,
    birthDate: string,
    preferredCategories: string[],
    skills: string[],
    isStudent: boolean,
    isAdmin: boolean
}
export default User;